import { RoomfilterPipe } from './roomfilter.pipe';

describe('RoomfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new RoomfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
